services:
  ai-tools:
    image: bouvet/quarkus-mcp:latest
    container_name: quarkus-mcp
    ports:
      - "8080:8080"
    environment:
      - QUARKUS_HTTP_HOST=0.0.0.0
      - QUARKUS_HTTP_PORT=8080
    restart: unless-stopped
